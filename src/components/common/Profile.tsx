import Logo from '@/components/common/Logo';

export default function Profile() {
  return (
    <div className="rounded-full bg-card size-20 p-2 flex items-center justify-center border border-muted">
      <Logo className="size-8" />
    </div>
  );
}
